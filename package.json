{
  "name": "freelio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Freelio - a full-stack freelance marketplace (React + Express + MySQL)",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "db:setup": "npm run db:setup --prefix server",
    "db:reset": "npm run db:reset --prefix server",
    "dev": "concurrently -n api,web -c cyan,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.11.3",
    "zod": "^3.23.8"
  }
}
